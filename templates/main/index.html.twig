{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}
{% block stylesheets %}
	{{ parent() }}
{% endblock %}
{% block body %}
	<main>
		{% for article in articles %}
			<h2>{{ article.titre }}</h2>
			<h3>{{ article.categorie.nom }}</h3>
			<p>publié le
				{{ article.date | date("d/m/Y")}}</p>
			<p>by <a href="{{ article.user.email }}">{{ article.user.pseudo }}</a>
			</p>
			<p>
				vues :
				{{article.nbreVues }}
			</p>
			<p>
				<a href="{{ path('app_article', { id: article.id }) }}">Lire</a>
			</p>
		{% endfor %}
	</main>
{% endblock %}
